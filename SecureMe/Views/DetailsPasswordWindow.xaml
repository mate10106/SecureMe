<Window x:Class="SecureMe.Views.DetailsPasswordWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Details Password" 
        Height="520" Width="620"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        FontFamily="Arial">
    <Window.Background>
        <LinearGradientBrush StartPoint="0,0" EndPoint="0,1">
            <GradientStop Color="#0F172A" Offset="0" />
            <GradientStop Color="#1E293B" Offset="1" />
        </LinearGradientBrush>
    </Window.Background>

    <Grid>
        <!-- Top Bar -->
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0" Height="50" Background="#1E293B">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <Button Content="✖" Width="30" Height="30" FontSize="16" 
                    Background="Transparent" Foreground="White"
                    BorderThickness="0" Cursor="Hand"
                    Click="CloseWindow_Click"/>

            <TextBlock Text="Details Password" Foreground="White" FontSize="18" 
                       VerticalAlignment="Center" HorizontalAlignment="Center"/>

            <Button Content="Edit" Width="60" Height="30" FontSize="14" 
                    Background="#374151" Foreground="White"
                    BorderThickness="0" Cursor="Hand" 
                    HorizontalAlignment="Right" Padding="5,2" Margin="5"/>
        </Grid>

        <!-- Content Section -->
        <StackPanel Grid.Row="1" Margin="40">
            <!-- Email -->
            <TextBlock Text="Email or Username" Foreground="Gray" FontSize="14"/>
            <TextBlock x:Name="EmailText" Text="" 
                       Foreground="White" FontSize="16" FontWeight="Bold" Margin="0,5,0,15"/>

            <!-- Password -->
            <TextBlock Text="Password" Foreground="Gray" FontSize="14"/>
            <Border Background="#111827" Padding="8" CornerRadius="5" Margin="0,5,0,10">
                <StackPanel Orientation="Horizontal">
                    <TextBlock x:Name="PasswordText" Text="••••••••" 
                               Foreground="White" FontSize="16" FontWeight="Bold"/>
                    <Button Content="👁" Background="Transparent" Foreground="White" 
                            BorderThickness="0" Cursor="Hand" Margin="10,0,0,0"
                            Click="TogglePassword_Click"/>
                </StackPanel>
            </Border>

            <!-- Password Health -->
            <TextBlock Text="Password Health" Foreground="Gray" FontSize="14"/>
            <StackPanel Orientation="Horizontal" Margin="0,5,0,10">
                <TextBlock Text="⚠ Weak Password" Foreground="#F87171" FontSize="14" FontWeight="Bold"/>
            </StackPanel>
            <StackPanel>
                <TextBlock Text="• Used for other 2 accounts" Foreground="White" FontSize="14"/>
                <TextBlock Text="• Over 90 days old" Foreground="White" FontSize="14"/>
            </StackPanel>

            <!-- Website Address -->
            <TextBlock Text="Website Address" Foreground="Gray" FontSize="14" Margin="0,15,0,0"/>
            <TextBlock Text="https://www.example.com" Foreground="#0EA5E9" FontSize="14"/>
        </StackPanel>
    </Grid>
</Window>
